{% set forgotLink %}
  {{ include('@atoms/link.html.twig', {
    'url': url('user.pass'),
    'title': 'Forgot your password?'|t,
  }) }}
{% endset %}

{% set registerLink %}
  {{ include('@atoms/link.html.twig', {
    'url': url('user.register'),
    'title': 'Create an account'|t,
  }) }}
{% endset %}

{{ include('@organisms/form/user-login.html.twig', {
  name: form.name,
  pass: form.pass,
  actions: form.actions,
  forgotLink: forgotLink,
  registerLink: registerLink,
  missingAccount: "Don't have an account yet?"|t,
}) }}

{{ form|without('name', 'pass', 'actions') }}
