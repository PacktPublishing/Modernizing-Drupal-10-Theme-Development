{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {{ include('@atoms/breadcrumb.html.twig', {
    'breadcrumb': breadcrumb,
  }) }}
{% endif %}
